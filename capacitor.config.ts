import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.multivendor.store',
  appName: 'MultiVendor Store',
  webDir: 'dist/frontend'
};

export default config;
